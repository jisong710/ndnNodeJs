import { crypto } from "@ndn/util";
import { IvGen } from "./gen_node.js";
/** IV generator using all random bits. */
export class RandomIvGen extends IvGen {
    generate() {
        return crypto.getRandomValues(new Uint8Array(this.ivLength));
    }
}
